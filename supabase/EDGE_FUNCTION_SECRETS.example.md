# Supabase Secret Template

Copy these values into Supabase Dashboard -> Project Settings -> Edge Functions
-> Secrets, or load them with `supabase secrets set --env-file`.

```env
SITE_URL=
N8N_INVITE_WEBHOOK_URL=
N8N_INVITE_WEBHOOK_SECRET=
ANTHROPIC_API_KEY=
```

